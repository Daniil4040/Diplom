<h1><%= @film.name %></h1>
<iframe width="560" height="315" src= <%= @film.trailer_link %> frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
<div class="info_film_page">
    <span class="lable">Рейтинг: </span><span class="value"><%= @film.rating %></span>
    <span class="lable">Год: </span><span class="value"><%= @film.year %></span>
    <span class="lable">Режиссёр: </span><span class="value"><%= @film.director %></span>
</div>
<hr>
<h2>Описание фильма "<%= @film.name %>"</h2>
<div class="descriptions_film">
    <% if @film.poster.attached? %>
        <%= image_tag @film.poster %>
    <% end %>
    <%= @film.description %>
</div>
<hr>
<p>
<h2>Отзывы об фильме "<%= @film.name %>":</h2>
</p>

<%= render 'comments/comments' %>
<% if user_signed_in? %>
    <%= render 'comments/form' %>
<% else %>
    <hr>
    <p> 
    Отзыв может оставлять только зарегестрированный пользователь
    </p>
<% end %>